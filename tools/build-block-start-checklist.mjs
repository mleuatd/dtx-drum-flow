import fs from "node:fs/promises";
const base="character-assets/prototypes/luna_say_maybe_16m";const start=Number(process.argv[2]),end=Number(process.argv[3]);
if(!Number.isInteger(start)||!Number.isInteger(end)||start<1||end<start)throw new Error("usage: node tools/build-block-start-checklist.mjs <start> <end> [out.json]");
const read=async p=>JSON.parse(await fs.readFile(p,"utf8"));const chart=await read("site/charts/luna_say_maybe/Luna_say_maybe_FINAL_notes.json");const limbs=await read("site/charts/luna_say_maybe/Luna_say_maybe_full_limbs.json");const map=await read(base+"/ACTION_KEY_ASSET_MAP.json");
const lm=new Map(limbs.assignments.map(a=>[Number(a.time).toFixed(6)+"|"+a.part,a.limb]));const notes=chart.notes.filter(n=>n.measure>=start&&n.measure<=end).map(n=>({...n,limb:lm.get(Number(n.time).toFixed(6)+"|"+n.part)}));
const groups=[];for(const n of notes){const g=groups.at(-1);if(g&&Math.abs(g[0].time-n.time)<=.008)g.push(n);else groups.push([n]);}
const key=g=>{const a=[...g].sort((x,y)=>x.part.localeCompare(y.part));return a.length===1?a[0].part+":"+a[0].limb:a.map(x=>x.part).join("+")+":"+a.map(x=>x.limb).join("/")};
const keys=[...new Set(groups.map(key))];const by=new Map(map.entries.map(e=>[e.actionKey,e]));const entries=keys.map(k=>by.get(k)||{actionKey:k,classification:"MISSING_MAPPING"});
const out={schemaVersion:1,generatedAt:new Date().toISOString(),range:{start,end},noteCount:notes.length,groupCount:groups.length,actionKeys:keys,reuse:entries.filter(e=>e.classification==="REUSE_APPROVED").map(e=>e.actionKey),pending:entries.filter(e=>e.classification==="REUSE_NEEDS_RUNTIME_QA").map(e=>e.actionKey),newAssets:entries.filter(e=>e.classification==="NEW_IMAGE_REQUIRED").map(e=>e.actionKey),blocked:entries.filter(e=>e.classification==="BLOCKED"||e.classification==="MISSING_MAPPING").map(e=>e.actionKey),dependencies:start>1?["previous contiguous measures through "+(start-1)+" complete"]:[],qaTargets:groups.map(g=>({measure:g[0].measure,time:g[0].time,actionKey:key(g)}))};
const dest=process.argv[4]||base+"/BLOCK_START_M"+start+"_"+end+".json";await fs.writeFile(dest,JSON.stringify(out,null,2)+"\n");console.log(JSON.stringify({dest,summary:{notes:out.noteCount,groups:out.groupCount,newAssets:out.newAssets.length,blocked:out.blocked.length}},null,2));